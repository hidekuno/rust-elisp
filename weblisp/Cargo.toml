[package]
name = "weblisp"
version = "0.6.2"
authors = ["hidekuno <hidekuno@gmail.com>"]
edition = "2018"

[dependencies]
chrono = "0.4"
elisp = { path = "../elisp", version = "0.6.2", features = ["thread"] }
log = "*"
env_logger = "*"

[features]
all-interface = []
